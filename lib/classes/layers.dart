import 'package:flutter/material.dart';
import 'package:flutter/cupertino.dart';

class Layers extends StatelessWidget{

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('hicgewiewi'),centerTitle: true,
      ),
    );
  }
}